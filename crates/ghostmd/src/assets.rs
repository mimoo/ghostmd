/// Placeholder for embedded application assets (icons, fonts, etc.).
///
/// During implementation this will use `rust_embed` to bundle
/// static assets into the binary, and implement `gpui::AssetSource`.
pub struct Assets;
